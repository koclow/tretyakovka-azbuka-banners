# L-board для телеканала РБК (код формата AIR_LB)

Источник требований — `../ТТ РБК/Техтребования - ТЕЛЕКАНАЛ РБК/ТТ_ТЕЛЕКАНАЛ РБК_05.pdf`,
страница «L-борд» (`tt-lboard-page.png`, превью зон — `tt-lboard-preview.png`).

## Что сдаём

| Параметр | Значение |
|---|---|
| Файл | MOV, ProRes 4444 |
| Кадр | 1920 × 1080 px, 50 progressive |
| Хронометраж | 10, 15 и 20 с — три файла |
| Звук | не требуется (в ТТ L-борда аудио не указано; требования к звуку относятся к MXF-роликам) |
| Анимация | да |

## Геометрия кадра (по превью РБК)

- Баннер занимает весь кадр 1920 × 1080; фон сплошной.
- Сверху накладывается программа: прямоугольник 600…1920 × 0…724 (1320 × 724).
- Снизу накладывается бегущая строка РБК: 96…1824 × 960…1044 (1728 × 84).
- Зрителю видно: левая полоса 600 px на всю высоту и нижняя полоса 356 px на всю ширину.
- Безопасная зона для текста и главных элементов: вертикаль 504 × 912 от (96, 48);
  низ 1728 × 236 от (96, 724). Поля 96 слева/справа, 48 сверху, 36 снизу видны, но вне зоны.

Шаблон с заглушками — в Figma, страница «Баннеры», фрейм
«L-board · шаблон 1920×1080 (РБК ТВ, AIR_LB)» (`figma-template.png`). Служебные слои заперты.

## Макет и сборка

Макет Никиты — фрейм «L-board · шаблон 2» (2037:987) на странице «Баннеры». Слои: плашка
«в компании художников» (104, 108), заголовок в три строки (106, 194), подзаголовок про
приложение (105, 395), логотип (104, 825); в нижней полосе конь (718, 762), круг с горами
(1169, 595 — верхом уходит под программу, так задумано) и пара (1633, 741).

Слои в `assets/`: тексты, плашка и логотип — SVG из Figma (`assets/svg/`, у экспорта в начале
два фоновых `<rect>` — вырезаны); заголовок — три копии одного SVG с `clipPath` по полосам строк,
границы полос лежат в пустых промежутках между строками, поэтому все три стоят на одном `top`.
Иллюстрации — PNG @4x из временного фрейма без заливки, серая подложка канвы выбита по цвету.
PNG-экспорт Figma всегда с подложкой (фон фрейма или канвы), поэтому текст через PNG @2x
получался мягким после даунскейла — Никита это заметил на финале, отсюда SVG.

`index.html` — сцена 1920×1080. Вход элементов — язык утверждённых билбордов (иллюстрации
снизу, плашка → строки заголовка → подзаголовок → логотип со «смазом» слева, всё за 2,7 с).
Дальше покой, вариант выбирается параметром `?v=`. Все анимации покоя стартуют после входа
(задержка 2,8 с и больше, `both`) и трогают только `transform`/`opacity` — кроме блика в `C`:

- `A` «значок на лацкане» — все три иллюстрации медленно качаются вокруг собственного центра
  (`transform-origin:50% 50%`): конь ±6° с периодом 11 с, круг с горами ±5° / 9,5 с (первый ход
  в другую сторону), пара ±6,5° / 13 с. Керфреймы 0 → +A → 0 → −A → 0 с `ease-in-out` между
  точками: на краях полная остановка и мягкий старт обратно, разворотов «на скорости» нет.
  Периоды и направления разные, поэтому значки не качаются синхронно. Текст и логотип стоят;
- `B` «наезд» — каждая иллюстрация растёт от своей нижней кромки (`transform-origin:50% 100%`)
  до 110 % за 17 с. Круг с горами идёт фронтально (только scale), пара при увеличении уходит
  на 16 px вправо, конь — на 14 px влево. Текст стоит;
- `C` «качание + блик» — A плюс белый диагональный блик по плашке «в компании художников».
  Узкая полоса 150 px с растушёвкой наклонена на −38° и идёт из левого нижнего угла плашки
  в правый верхний (ход ±215 px), маска — `assets/badge.png`, поэтому блик ложится и на золотую
  подложку, и на текст. Проход 1,6 с по `cubic-bezier(.65,0,.35,1)` — разгон в середине, мягкие
  края; два прохода, серединой на 7-й и на 13-й секунде (цикл 6 с × 2, задержка 6,2 с).
  Прежний золотой блик по логотипу убран.

`?overlay` рисует заглушки эфира и бегущей строки — для черновика на согласование.
Ухода нет: последний кадр держится до конца, эфир сам снимает баннер.

## Как рендерится

Та же схема, что у HTML-баннеров: слои и CSS-анимация в `index.html`, затем
`tools/render.js` гонит headless Chrome по кадрам (50 в секунду, `document.getAnimations()`
ставится на нужное время) и отдаёт их ffmpeg в ProRes 4444.

```bash
cd lboard/tools && npm i          # один раз, puppeteer-core берёт установленный Chrome
node render.js ../index.html 20 ../dist/draft_A_20s.mp4 25 0 "v=A&overlay"   # черновик, H.264, ~1 МБ
node render.js ../index.html 20 ../dist/lboard_20s.mov 50 0 "v=A"            # сдача, ProRes 4444
```

Если файл `.mp4` — H.264 (crf 20, yuv420p), иначе ProRes 4444; пятый аргумент `1` включает альфу.
Хронометраж задаётся вторым аргументом, кадры считаются точно: 20 с × 50 = 1000 кадров.

Пилот 2 с (`pilot.html`) — 100 кадров за 8 с; 20-секундный файл ≈ 1,5 минуты. Проверка:
`ffprobe -show_entries stream=codec_name,profile,width,height,r_frame_rate,nb_frames <файл>`.

## Сдача

Финальные файлы — `final/` (вне git): `lboard_C_20s.mov`, `lboard_C_15s.mov`, `lboard_C_10s.mov`
(ProRes 4444, 1920×1080, 50p, ровно 1000/750/500 кадров, без звука), `preview_C_20s.mp4` и
`README.txt` для заказчика. Выкладка на Яндекс.Диск с публикацией ссылки:

```bash
YD_TOKEN=$(secret get "Яндекс.Метрика — OAuth-токен 3.09.2026" --field password) \
python3 tools/ydisk_upload.py "Азбука вкуса × Третьяковка — материалы для РБК" \
  "1 · HTML-баннеры (сайт РБК)=../Отправка РБК" "2 · L-board (эфир телеканала)=final"
```

Повторный запуск перезаписывает файлы в той же папке, ссылка не меняется.
