# ЖУРНАЛ — Баннеры Третьяковка × Азбука вкуса

## 10.09.2026 — Claude (Fable), iMac — Никита (2) — маркировка на HTML-баннерах; сверка L-board с ТТ
Финалы L-board сверены с листом AIR_LB ТТ телеканала (MOV, ProRes 4444, 1920×1080, 50p, 10/15/20 с —
сходится, 500/750/1000 кадров, без звука) и по резкости на кадре 1:1 — чисто. Замечено: в ТТ левая
колонка 504 px, в референсе РБК эфирное окно с 599 — раскладка сделана по референсу; при строгих 504
пара зайдёт под окно на 55 px. Первый кадр — чистый фон.
По просьбе Никиты поставлена маркировка на четыре HTML-креатива и заглушки (см. DECISIONS 10.09):
`creatives/*/mk*.png` из `lboard/tools/mark2png.js`, заглушки пересняты `lboard/tools/fallback.js`,
`build-standalone.py` прошёл проверку ТТ, `Отправка РБК/` пересобрана (110/133/65/72 КБ; заглушки
58/50/30/29 КБ). Проверено скриншотами на 680/900/1440/1920 и 300/375 и кадрами появления
(1,9 / 2,3 / 2,9 с). Статус переписан: в нём стояла ревизия Codex от 08.09 14:25, написанная до
финалов раскладки 3.

Состояние до этой ревизии (сохранено целиком):

# PROJECT_STATUS — Баннеры Третьяковка × Азбука вкуса

## Ревизия
08.09.2026, Codex, iMac — Никита (2).

## Сейчас
HTML-баннеры согласованы; комплект — `Отправка РБК/`, витрина —
https://koclow.github.io/tretyakovka-azbuka-banners/.
L-board C ранее выбран заказчиком: ProRes 4444 на 20/15/10 с — `lboard/final/`;
материалы на Диске: https://yadi.sk/d/fE9P9FIQjx-HBg.
По новой просьбе Никиты доработана композиция «значки слева» в Figma:
https://www.figma.com/design/MuQyOuxDwWEzAb28gpTgQC/?node-id=2084-82.
Значки целиком в видимой области, блок бренда выровнен. Плашки эфира и строки
с подписями идентичны источнику 2081:1999. Проверено скриншотом и сравнением свойств.
Новая композиция пока только в Figma. Более новые, чем прошлая ревизия,
`lboard/index.html` и `lboard/dist/draft_C_L3_20s.mp4` существовали до текущих правок.

## Следующий шаг
Никита оценивает фрейм 2084:82; после выбора композиции — перенести её в анимацию.

## Открытые нити
- Н1 · L-board: новая композиция в Figma ожидает оценки; финальные MOV относятся к предыдущей версии. Требование к альфе области программы остаётся открытым.
- Н7 · Маркировка HTML: ждём решения Никиты; на L-board текст заказчика уже стоит.
- Н2 · Под `right_1_inner` (300×600) макета нет.
- Н3 · Дополнительные фотоформаты в Figma пока не собраны.
- Н4 · Проект пока в `00 Входящие`; при продолжении работы решить перенос.
- Н5 · Живые примеры перетяжки РБК не сняты из-за блокировщика.

## Проверено и отброшено
- Заливка гигабайта на Яндекс.Диск скриптом с аймака — 150 КБ/с и обрывы (маршрут через роутер
  и туннель), до Maura при этом 6 МБ/с. Крупные файлы проще залить руками из браузера или через
  Maura как перевалку; скрипт оставлен для мелких файлов и публикации ссылки.
- Тексты L-board из PNG @2x — на финальном MOV края мягкие (Никита: «видны пиксели, особенно
  плашка»). Переведены на SVG в сцене (три строки — clipPath), иллюстрации на PNG @4x, финал рендерится
  с суперсэмплингом 2× (`SS=2`, даунскейл Lanczos). Больше из 1080p не выжать — это предел ТТ.
- Нейровидео Kling 3.0 (image-to-video, first_image, 14 с, 1080p) — две попытки, 4 генерации,
  448 кредитов. Вся полоса целиком: значки крутятся как монеты, фон шумит. По одному значку
  в квадрате с сюжетными промптами («конь перебирает ногами», «свет на горах меняется»,
  «пара чуть ближе»): сюжеты ожили, но модель перерисовывает графику в свою манеру (у всадника
  другое лицо и волосы, значок деформируется, у круга глянцевый 3D-ободок), фон с текстурой.
  Никита: «видосы не подходят». Ключевание фона по RGB работало чисто, но не понадобилось.
  Файлы `lboard/dist/kling_*_14s.mp4` оставлены для справки.
- PNG-экспорт Figma (`download_assets`) всегда с подложкой — фон фрейма или серый канвы, даже
  из фрейма без заливки. Для слоёв с альфой брать SVG-экспорт и растеризовать самим; у SVG
  из Figma тоже два фоновых `<rect>` в начале — вырезать.
- Маска `mask-image: url(...)` из `file://` в headless Chrome блокируется CORS — флаг
  `--allow-file-access-from-files` в рендере.
- Скриншот puppeteer с `clip` при остановленных анимациях (`?at=`) отдаёт один и тот же кадр
  при любом времени — снимать без `clip`, вьюпорт ровно под размер креатива.
- SVG в архиве — ТТ РБК: «не поддерживаются svg и json». Отрисованы в PNG headless-Chrome.
- Подпапка `assets/` в архиве — ТТ: «без дополнительных папок внутри». Файлы рядом с html.
- Пути с `300x250` / папка `banners/` — блокировщик в Chrome Никиты подменял картинки на 1×1
  (fetch → 500 без заголовков GitHub, curl с той же машины → 200). Переименовано.
- Старт по IntersectionObserver + таймер 1,5 с при видимых слоях — кадр мелькал и исчезал
  («дёрганая первая загрузка»). Слои спрятаны до `play`, старт по `load` и видимости.
- «Смаз» 16 px / scaleX 1.08 / blur 7 px — слишком акцентно по оценке Никиты; теперь 10 / 1.03 / 4.
- Мобильный 300×250 по макету «4» (фото сверху) — заменён новым макетом «iPhone 16».
- Появление заголовка маской слева направо (`clip-path`) — Никита: «как PowerPoint».
  Заменено на пословный «смаз» в языке фирменного ролика.
- Резиновый мобильный 100%×250 с фото во всю ширину — ТТ просит файл 300×250; поля в
  слоте заливает сам РБК. Заменён фиксированным 300×250.
- Headless Chrome для скриншотов: `--virtual-time-budget` с бесконечными CSS-анимациями
  виснет; окно уже 500 px даёт вьюпорт шире экрана (баннер уезжает вправо) — снимать
  окном ≥ 500 и вырезать.
- Раскладка «только набор» сразу ниже 680 px — на планшете 680 слот РБК-страницы
  получается 664–678 px, и билборд проваливался в мобильный вид. От 600 до 679 теперь
  макет 680 по центру с подрезкой.
- Кэш CDN GitHub Pages (10 минут на URL) отдавал старые креативы в схеме страницы —
  рамка пробрасывает версию в адреса креативов.
- `%` в формулах интерполяции (`calc(100% - 680px)`) — в `top` проценты считаются от
  высоты контейнера, конь уезжал на 44 px вниз. Заменено на `100cqw`.
- Заголовок, растущий линейно от 680 — к 800 px упирался в голову коня. Растёт с 960 px.
- SVG иллюстрации из Figma (конь 136 КБ, круг 112, пара 100) — втрое тяжелее лимита,
  оставлены PNG @3x в палитре.
- Старт билборда только по IntersectionObserver — в фоновой вкладке Chrome не крутит
  рендеринг, наблюдатель и scroll молчат, слот оставался пустым. Теперь ещё
  visibilitychange + таймер 1,5 с (креатив вставляет сам слот РБК, когда пора), а на
  схеме страницы второй слот опрашивается таймером. Проверено в фоновой вкладке.
- Живые примеры баннеров на rbc.ru снять не удалось: и во встроенном браузере, и в
  Chrome Никиты слоты пустые (0×0, блокировщик). Официальные ТТ и медиакит —
  на rbc.group/advertisers («Технические требования ZIP», «Смотреть медиакит»).
- Живой текст ближайшим гугл-шрифтом — набор расходится с макетом.
- Блик-«шайн» раз в 9 с и построчное покачивание набора — убраны по аудиту анимации.
- Резиновый режим у 680×250 (`?mode=fluid`) — заменён отдельным билбордом, который
  делает то же самое и вдобавок умеет мобильную раскладку.
- Пропорциональное сжатие настольной композиции на мобильном слоте 100%×250 — при
  375 px даёт высоту 138 px и нечитаемый кегль. Вместо этого квадратная раскладка.
- `padding` на контентной коробке для «дышащих» полей — у абсолютных детей
  containing block это padding box, отступ на них не действует.
- CSS-транзишен ширины стенда — анимировать layout плохо, и `transitionend`
  не приходил в связке с `resize:horizontal`.
- Figma MCP: `get_metadata` по узлам обоих файлов падает (SSE обрывается).
  Работали через `get_screenshot` + `download_assets` @3x и разбор по связным компонентам.
- Приватный репозиторий + Pages — план аккаунта не поддерживает.
- Figma loadFontAsync для AzbukaVkusa Regular недоступен. Для композиционных правок клонировать исходник и сохранять фирменные текстовые слои без изменения; замена шрифта не нужна.

## Ждёт Никиту / блокеры
- Оценка доработанной композиции L-board (2084:82).
- Маркировка HTML, формат 300×600 и остальные фотоформаты.

## Условие смерти ветки
Не задано.


## 08.09.2026 — Codex (iMac — Никита (2)) — композиция L-board в Figma

По просьбе Никиты доработан вариант «значки слева». Создан отдельный фрейм
2084:82 справа от вариантов: https://www.figma.com/design/MuQyOuxDwWEzAb28gpTgQC/?node-id=2084-82
Источник — 2081:1999. Сближена нижняя группа, горы опущены, конь уменьшен до
габарита 234×220 и размещён целиком между эфиром и строкой; логотип и маркировка
выровнены по правому краю x1800. Плашки эфира, бегущей строки и обе их подписи
сверены с источником по геометрии, видимости, заливкам и тексту: совпадают.
Результат проверен скриншотом. Исходные варианты сохранены. Шрифт AzbukaVkusa
недоступен для loadFontAsync; клонирование без изменения фирменных текстов работает.
Новая композиция пока только в Figma, в HTML и финальные MOV не переносилась.
При ревизии обнаружены более новые lboard/index.html и draft_C_L3_20s.mp4;
содержание этой предшествующей работы не проверялось и не изменялось.

Состояние до этой ревизии (сохранено целиком):

# PROJECT_STATUS — Баннеры Третьяковка × Азбука вкуса

## Ревизия
08.09.2026, Claude (Fable), iMac — Никита (2).

## Сейчас
Опубликовано: <https://koclow.github.io/tretyakovka-azbuka-banners/>, показ заказчику —
`demo/rbc.html` (экраны 375 / 680 / 1440 / 1920; десктоп — сверху иллюстрация, по доскролу
фото; мобильный — сверху женщина, по доскролу мужчина).

Баннеры согласованы Никитой. К отправке собрана папка `Отправка РБК/`: две перетяжки 100%×250
(архивы 88 и 122 КБ, заглушки 680×250 — 62 и 56 КБ), два мобильных 300×250 по макету «iPhone 16»
(56 и 62 КБ, заглушки 34 и 34 КБ) и README.txt. Сверено с официальным листом «HTML» ТТ
(`ТТ РБК/…/TT_RU_v7.08.26.xlsx`): архивы плоские, без svg (тексты и логотипы — PNG), имена
латиницей, клик по макросу `%banner.reference_mrc_user1%`; проверку делает `build-standalone.py`.
Запасной сплошной 300×250 — 19 КБ, не отправляется.
L-board (эфир телеканала, код AIR_LB): макет Никиты (фрейм 2037:987) собран в `lboard/index.html`,
вход в языке билбордов, три варианта покоя (`?v=A|B|C`: значки качаются ±5–6,5° с плавным реверсом / наезд до 110 %
с разъездом / качание + белый диагональный блик по плашке — вторая итерация после правок Никиты). Заказчик выбрал C. Финал отрендерен: `lboard/final/lboard_C_{20,15,10}s.mov` — ProRes 4444,
1920×1080, 50p, 1000/750/500 кадров, без звука (457/340/224 МБ, вне git) + `preview_C_20s.mp4`.
Материалы выложены на Яндекс.Диск (`lboard/tools/ydisk_upload.py`): папка «Азбука вкуса ×
Третьяковка — материалы для РБК» с HTML-баннерами и L-board: <https://yadi.sk/d/fE9P9FIQjx-HBg>.
Состав и md5 всех файлов сверены по API Диска.

Пути переименованы без рекламных маркеров (`creatives/desktop-illustration`, `…/desktop-photo`,
`…/mobile-woman`, `…/mobile-man`, `…/square-plain`; корневой класс `.cr`): в Chrome Никиты
блокировщик резал ассеты по `300x250`.

Геометрия по обновлённым макетам: «вар 2» — заголовок 452 px и логотип 210 px ниже и меньше,
круг верхом за кадр; «вар 1» — лок-ап 314 px. Анимация в языке фирменного ролика, амплитуды
спокойные; старт без вспышки (слои спрятаны до `play`, старт по `load` при видимой вкладке,
`done` через 3 с).

## Следующий шаг
Никита отправляет `Отправка РБК/` заказчику (ссылкой на скачивание, не вложением — ТТ). Перед этим
решить про маркировку (см. «Ждёт Никиту»). Никита выбирает вариант покоя
L-board (A / B / C) и отправляет черновик заказчику; после «ок» — правки, ProRes 4444 на 20, 15 и 10 с.

## Открытые нити
- Н1 · L-board — черновики готовы; открыт вопрос, нужна ли альфа в области программы (сдаём сплошной
  фон на весь кадр — работает в обоих случаях). Для 15 и 10 с вход тот же, покой короче.
- Н6 · Статичные версии: заглушки 680×250 и 300×250 лежат в `Отправка РБК/` рядом с архивами.
- Н7 · Маркировка рекламы — заказчик дал текст, на L-board стоит; для HTML-баннеров ждём решения Никиты: по общим ТТ на креативе должно быть наименование юрлица рекламодателя
  или адрес сайта; в макетах этого нет (в «вар 2» есть скрытый слой «Реклама. ERID…» из другого
  проекта). Плашку «Реклама» РБК накладывает сам, ОРД-токен по умолчанию регистрирует РБК.
- Н2 · Под слот `right_1_inner` (300×600) макета нет.
- Н3 · В файле Figma лежат ещё форматы с фотографиями (Директ, ВК, 728×90) — собираются тем же способом.
- Н4 · Проект лежит в `00 Входящие`. Если работа продолжится — переселить.
- Н5 · Живые примеры перетяжки на rbc.ru снять не удалось (блокировщик) — ссылки отданы Никите.

## Проверено и отброшено
- Заливка гигабайта на Яндекс.Диск скриптом с аймака — 150 КБ/с и обрывы (маршрут через роутер
  и туннель), до Maura при этом 6 МБ/с. Крупные файлы проще залить руками из браузера или через
  Maura как перевалку; скрипт оставлен для мелких файлов и публикации ссылки.
- Тексты L-board из PNG @2x — на финальном MOV края мягкие (Никита: «видны пиксели, особенно
  плашка»). Переведены на SVG в сцене (три строки — clipPath), иллюстрации на PNG @4x, финал рендерится
  с суперсэмплингом 2× (`SS=2`, даунскейл Lanczos). Больше из 1080p не выжать — это предел ТТ.
- Нейровидео Kling 3.0 (image-to-video, first_image, 14 с, 1080p) — две попытки, 4 генерации,
  448 кредитов. Вся полоса целиком: значки крутятся как монеты, фон шумит. По одному значку
  в квадрате с сюжетными промптами («конь перебирает ногами», «свет на горах меняется»,
  «пара чуть ближе»): сюжеты ожили, но модель перерисовывает графику в свою манеру (у всадника
  другое лицо и волосы, значок деформируется, у круга глянцевый 3D-ободок), фон с текстурой.
  Никита: «видосы не подходят». Ключевание фона по RGB работало чисто, но не понадобилось.
  Файлы `lboard/dist/kling_*_14s.mp4` оставлены для справки.
- PNG-экспорт Figma (`download_assets`) всегда с подложкой — фон фрейма или серый канвы, даже
  из фрейма без заливки. Для слоёв с альфой брать SVG-экспорт и растеризовать самим; у SVG
  из Figma тоже два фоновых `<rect>` в начале — вырезать.
- Маска `mask-image: url(...)` из `file://` в headless Chrome блокируется CORS — флаг
  `--allow-file-access-from-files` в рендере.
- Скриншот puppeteer с `clip` при остановленных анимациях (`?at=`) отдаёт один и тот же кадр
  при любом времени — снимать без `clip`, вьюпорт ровно под размер креатива.
- SVG в архиве — ТТ РБК: «не поддерживаются svg и json». Отрисованы в PNG headless-Chrome.
- Подпапка `assets/` в архиве — ТТ: «без дополнительных папок внутри». Файлы рядом с html.
- Пути с `300x250` / папка `banners/` — блокировщик в Chrome Никиты подменял картинки на 1×1
  (fetch → 500 без заголовков GitHub, curl с той же машины → 200). Переименовано.
- Старт по IntersectionObserver + таймер 1,5 с при видимых слоях — кадр мелькал и исчезал
  («дёрганая первая загрузка»). Слои спрятаны до `play`, старт по `load` и видимости.
- «Смаз» 16 px / scaleX 1.08 / blur 7 px — слишком акцентно по оценке Никиты; теперь 10 / 1.03 / 4.
- Мобильный 300×250 по макету «4» (фото сверху) — заменён новым макетом «iPhone 16».
- Появление заголовка маской слева направо (`clip-path`) — Никита: «как PowerPoint».
  Заменено на пословный «смаз» в языке фирменного ролика.
- Резиновый мобильный 100%×250 с фото во всю ширину — ТТ просит файл 300×250; поля в
  слоте заливает сам РБК. Заменён фиксированным 300×250.
- Headless Chrome для скриншотов: `--virtual-time-budget` с бесконечными CSS-анимациями
  виснет; окно уже 500 px даёт вьюпорт шире экрана (баннер уезжает вправо) — снимать
  окном ≥ 500 и вырезать.
- Раскладка «только набор» сразу ниже 680 px — на планшете 680 слот РБК-страницы
  получается 664–678 px, и билборд проваливался в мобильный вид. От 600 до 679 теперь
  макет 680 по центру с подрезкой.
- Кэш CDN GitHub Pages (10 минут на URL) отдавал старые креативы в схеме страницы —
  рамка пробрасывает версию в адреса креативов.
- `%` в формулах интерполяции (`calc(100% - 680px)`) — в `top` проценты считаются от
  высоты контейнера, конь уезжал на 44 px вниз. Заменено на `100cqw`.
- Заголовок, растущий линейно от 680 — к 800 px упирался в голову коня. Растёт с 960 px.
- SVG иллюстрации из Figma (конь 136 КБ, круг 112, пара 100) — втрое тяжелее лимита,
  оставлены PNG @3x в палитре.
- Старт билборда только по IntersectionObserver — в фоновой вкладке Chrome не крутит
  рендеринг, наблюдатель и scroll молчат, слот оставался пустым. Теперь ещё
  visibilitychange + таймер 1,5 с (креатив вставляет сам слот РБК, когда пора), а на
  схеме страницы второй слот опрашивается таймером. Проверено в фоновой вкладке.
- Живые примеры баннеров на rbc.ru снять не удалось: и во встроенном браузере, и в
  Chrome Никиты слоты пустые (0×0, блокировщик). Официальные ТТ и медиакит —
  на rbc.group/advertisers («Технические требования ZIP», «Смотреть медиакит»).
- Живой текст ближайшим гугл-шрифтом — набор расходится с макетом.
- Блик-«шайн» раз в 9 с и построчное покачивание набора — убраны по аудиту анимации.
- Резиновый режим у 680×250 (`?mode=fluid`) — заменён отдельным билбордом, который
  делает то же самое и вдобавок умеет мобильную раскладку.
- Пропорциональное сжатие настольной композиции на мобильном слоте 100%×250 — при
  375 px даёт высоту 138 px и нечитаемый кегль. Вместо этого квадратная раскладка.
- `padding` на контентной коробке для «дышащих» полей — у абсолютных детей
  containing block это padding box, отступ на них не действует.
- CSS-транзишен ширины стенда — анимировать layout плохо, и `transitionend`
  не приходил в связке с `resize:horizontal`.
- Figma MCP: `get_metadata` по узлам обоих файлов падает (SSE обрывается).
  Работали через `get_screenshot` + `download_assets` @3x и разбор по связным компонентам.
- Приватный репозиторий + Pages — план аккаунта не поддерживает.

## Ждёт Никиту / блокеры
- Макет L-board в Figma (шаблон готов).
- Решение по 300×600 и по остальным фотоформатам из нового файла Figma.
- Официальные ТТ РБК лежат на rbc.group/advertisers («Технические требования ZIP») —
  сверить с присланной таблицей целиком, если заказчик даст полный документ.

## Условие смерти ветки
Не задано.


Свежее сверху.

- 08.09.2026, вечер — Claude (Fable), iMac — заказчик согласовал раскладку 3 по композиту с эфиром
  РБК; финалы ProRes 4444 20/15/10 с перерендерены (SS=2), README для папки обновлён; Никита
  заливает на Диск сам.

- 08.09.2026 — Claude (Fable), iMac — правки заказчика по L-board: значки уменьшены до 208 px и
  расставлены в видимой полосе (макет Figma обновлён), добавлена строка маркировки (ООО «Городской
  супермаркет», ИНН), заглушка строки как на скриншоте РБК; черновик MP4 v2; Никита: слишком мелко —
  v3 с заходом значков за край не больше 10 % высоты (круг под эфир, конь и пара под строку);
  затем раскладка 2 по новому макету Никиты (2077:456), выровнена и собрана как `?l=2`; третья раскладка (2077:839, иллюстрации сверху,
  текст в углу) — выровнена, Никита дособрал и выбрал её; сцена `?l=3` обновлена под его макет,
  черновик MP4 на согласование заказчику; логотип пересобран из подрезанного блока. РБК прислали
  образец эфира (сжатие, строка, поля) — снята геометрия и тайминги, `tools/rbc_composite.py`
  собирает наш баннер в их ролик.

- 04.09.2026, поздний вечер — Claude (Fable), iMac — заказчик выбрал L-board C. Финал ProRes 4444
  20/15/10 с отрендерен и проверен по кадрам; HTML-баннеры и L-board выложены на Яндекс.Диск:
  <https://yadi.sk/d/fE9P9FIQjx-HBg> (аккаунт nk@proektoria.media). Финалы перерендерены из SVG с
  суперсэмплингом 2×; крупные MOV Никита залил руками — из дома в Яндекс через роутер идёт
  150 КБ/с, скрипт годится для мелких файлов. Состав и md5 сверены по API.

- 04.09.2026, день — Claude (Fable), iMac — Никита отклонил первые варианты покоя L-board
  (дыхание не видно, наезд мал, блик прямолинейный). Переделка агентом на Opus: повороты значков
  ±5–10° с плавным реверсом, наезд до 110 % с разъездом, белый диагональный блик по плашке.
  Нейровидео Kling 3.0: сначала полоса целиком, затем три значка по отдельности с сюжетными
  промптами — отклонено Никитой (перерисовка графики, шум фона), см. статус. Остаёмся на CSS-вариантах A/B/C.

- 04.09.2026, вечер — Claude (Fable), iMac — L-board: макет 2037:987 разобран, слои вытащены (SVG →
  PNG с альфой), `lboard/index.html` со входом билбордов и тремя вариантами покоя, три черновика
  20 с H.264 с заглушками эфира и строки для согласования.

- 04.09.2026, вечер — Claude (Fable), iMac — баннеры согласованы; финальная сверка с листом «HTML»
  официальных ТТ: SVG → PNG, плоские архивы, имена латиницей, ссылка-клик по макросу РБК; проверка
  в `build-standalone.py`; собрана `Отправка РБК/` (4 архива + 4 заглушки + README.txt);
  раскадровки всех креативов пересняты. Выявлен вопрос маркировки (юрлицо/сайт на креативе).

- 04.09.2026, день — Claude (Fable), iMac — ТТ РБК (интернет + телеканал) сложены в `ТТ РБК/`;
  наши форматы по интернет-ТТ сошлись (архив ≤ 300 КБ, заглушки 85/65 КБ). L-board: разобрана
  геометрия зон, в Figma создан шаблон с заглушками (фрейм 2035:64), пилот рендера
  HTML → ProRes 4444 (`lboard/tools/render.js`), `lboard/README.md`.

- 03.09.2026, ночь — Claude (Fable), iMac — в Chrome Никиты мобильные были зелёными: блокировщик
  по пути `300x250`; папки и классы переименованы (`creatives/…`, `.cr`). Макеты обновлены:
  «вар 2» (заголовок и логотип меньше и ниже, круг выше), «вар 1» (лок-ап 314), новый мобильный
  «iPhone 16» (фото слева, резиновый от 300). Старт без вспышки (слои спрятаны до `play`, старт
  по `load` и видимости, `done`), «смаз» спокойнее (10 px / 3 % / 4 px).

- 03.09.2026, поздний вечер — Claude (Fable), iMac — сверка с ТТ по скриншотам; анимация
  переписана в языке фирменного ролика (иллюстрация снизу, слова и логотип со «смазом»,
  по одному); мобильные переделаны в 300×250 по ТТ; демо упрощено (убраны переключатели
  креативов и ссылка «к баннерам»); заглушки для всех четырёх креативов; `?at=` для
  раскадровки; DECISIONS и ЖУРНАЛ заведены.
- 03.09.2026, вечер — Claude, iMac — пересборка по макетам Никиты (файл
  `MuQyOuxDwWEzAb28gpTgQC`): билборды А и Б на 1440 с линейной интерполяцией к 680,
  мобильные с фото, медленная анимация, демо с экранами 375 / 680 / 1440 / 1920.
- 03.09.2026, день — Claude, iMac — первые баннеры 680×250 и 300×250 из первого макета,
  GitHub Pages, стенд ресайза, аудит анимации, схема страницы РБК с настоящими
  названиями слотов (`data-unit`).
